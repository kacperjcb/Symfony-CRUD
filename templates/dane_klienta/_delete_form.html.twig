<form method="post" action="{{ path('app_dane_klienta_delete', {'id': dane_klientum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dane_klientum.id) }}">
    <button class="btn">Delete</button>
</form>
