{% extends 'base.html.twig' %}

{% block title %}ClientInfo{% endblock %}

{% block body %}
    <h1>ClientInfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_info.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ client_info.Name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ client_info.Surname }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ client_info.City }}</td>
            </tr>
            <tr>
                <th>Kod Pocztowy</th>
                <td>{{ client_info.PostCode }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ client_info.Address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_Client_Info_index') }}">back to list</a>

    <a href="{{ path('app_client_info_edit', {'id': client_info.id}) }}">edit</a>

    {{ include('client_info/_delete_form.html.twig') }}
{% endblock %}
